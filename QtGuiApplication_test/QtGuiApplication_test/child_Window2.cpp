#include "ui_childwindow2.h"
#include "child_Window2.h"

child_window2::child_window2(QWidget *parent) :QWidget(parent)
{
	//this->setWindowTitle("文件输入输出-文件夹选择");
	//backtomainwindow.setParent(this);   //设置按钮的父对象为子窗口
	//connect(&backtomainwindow, &QCommandLinkButton::clicked, this, &child_window1::sendmysignal);

}
//
//void child_window1::sendmysignal()   //定义槽函数
//{
//	emit mysignal();
//	this->hide();
//}
